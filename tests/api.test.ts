import { test, describe } from 'vitest';

describe('api returns expected data', () => {
	test('/carMPG/[year]', async () => {
		await Promise.resolve(() => {
			console.error('aaaaaaaaaaaaaaaaaa');
		});
	});
});
